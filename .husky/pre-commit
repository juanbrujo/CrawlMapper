#!/usr/bin/env sh

# Run tests only if test files exist
if find . -name "*.test.*" -o -name "*.spec.*" | grep -v node_modules | head -1 | read; then
  npm test
fi

# Run linting
npm run lint

# Check formatting
npm run format:check